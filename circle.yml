machine:
  environment:
    LC_CTYPE: en_US.UTF-8

dependencies:
  override:
    - sudo gem install xcpretty
    - brew install carthage
    - carthage update --use-submodule --platform iOS
    - /var/folders/jm/fw86rxds0xn69sk40d18y69m0000gp/T/*.log

test:
  override:
    - make test && cp build/test-report.xml $CIRCLE_TEST_REPORTS
