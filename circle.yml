machine:
  environment:
    LC_CTYPE: en_US.UTF-8

dependencies:
  override:
    - sudo gem install xcpretty
    - brew install carthage
    - carthage update --use-submodule --verbose --platform Mac

test:
  override:
    - make test && cp build/test-report.xml $CIRCLE_TEST_REPORTS
